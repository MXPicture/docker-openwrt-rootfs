name: Check Changes

on:
#  schedule:
#    # 01:00 UTC each day
#    - cron:  '0 1 * * *'
    
  workflow_dispatch:
  
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: View context attributes
        uses: actions/github-script@v5
        with:
          script: console.log(context)
      - name: Get branches & Dispatch events in order to deploy docker images
        uses: actions/github-script@v5
        with:
          script: |
            const branches = $(https://api.github.com/repos/MXPicture/docker-openwrt-rootfs/branches)
            console.log(branches)
            
