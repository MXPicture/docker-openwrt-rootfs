name: Check Changes

on:
#  schedule:
#    # 01:00 UTC each day
#    - cron:  '0 1 * * *'
    
  workflow_dispatch:
  
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: get target tags
        uses: CamiloGarciaLaRotta/watermelon-http-client@v1
        with:
          url: "https://hub.docker.com/v2/repositories/mxpicture/openwrt-rootfs"
          headers:
            accept: "application/json"

      - name: Get tags, compare docker hub image dates and trigger deploy
        uses: actions/github-script@v5
        with:
          script: |
            const tags = (await github.request("https://api.github.com/repos/" 
              + context.repo.owner + "/" + context.repo.repo + "/tags")).data

          

            console.log(tarImages)
            for (const tag of tags) {
              console.log(tag.name)

            }
